import { reactive as f, ref as l, getCurrentInstance as p, watch as S, nextTick as y, onBeforeUpdate as I } from "vue";
function x({
  idProp: o = (n) => n.item.id
} = {}) {
  const n = f({}), r = l(null);
  let i = null;
  const e = p();
  if (!e)
    throw new Error("[useIdState] Must be called inside setup()");
  const a = typeof o == "function" ? () => o(e.proxy) : () => e.proxy[o];
  function d(t) {
    const u = e.proxy.$options.idState;
    if (typeof u == "function") {
      const s = u.call(e.proxy, e.proxy);
      return n[t] = s, i = t, s;
    } else
      throw new TypeError("[useIdState] Missing `idState` function on component definition.");
  }
  function c() {
    const t = a();
    t == null && console.warn(`No id found for IdState with idProp: '${o}'.`), t !== i && (n[t] || d(t), r.value = n[t]);
  }
  return S(a, (t) => {
    y(() => {
      i = t;
    });
  }, { immediate: !0 }), c(), I(() => {
    c();
  }), {
    idState: r
  };
}
export {
  x as useIdState
};
//# sourceMappingURL=useIdState.js.map
